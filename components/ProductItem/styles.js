import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    breadItem: {
        padding: 20,
        margin: 10,
        borderRadius: 3,
        backgroundColor: '#ccc'
    },
    title: {
        fontSize: 20,
    },
    details: {
        fontSize: 18,
    },
});